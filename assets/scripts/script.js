$(document).ready(function() {
	let total = 
	$('.spisok-3-item-br').click(function(){
	let value = '.value' // создаём переменную для вычесленний
	valve = valve.valve + 1 //добавляем к переменной "valve" 1
	})
});